<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/test.css">
    <title>Document</title>
</head>
<body class="grey-bg">
    <%- include('nav.ejs') %>
    <div class="white-bg">
        
        <% for(let i=0;i<result.length;i++){ %>
            <div class="list-box">
                <a href='/detail/<%= result[i]._id %>''><%= result[i].title %></a>
                <p><%= result[i].content %></p>
                <a href="/edit/<%= result[i]._id%>">✏️</a>
              </div>
        <% } %>
      </div> 
</body>
</html>